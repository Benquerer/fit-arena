import React from 'react'
import { Start } from '../components'

export const Home = () => {
  //loads the start component that represents the home page content
  //this is "unecessary" at first look, but just like the site is planned to expand, so is the homepage
  return (
    <Start></Start>
  )
}
